<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
   xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
   xmlns:context="http://www.springframework.org/schema/context"
   xsi:schemaLocation="http://www.springframework.org/schema/beans http://www.springframework.org/schema/beans/spring-beans.xsd
      http://www.springframework.org/schema/context http://www.springframework.org/schema/context/spring-context-4.3.xsd">

   <context:property-placeholder
      location="/WEB-INF/props/jdbc.properties" />

   <bean id="dataSource"
      class="org.apache.commons.dbcp2.BasicDataSource"
      destroy-method="close">
      <property name="username" value="${jdbc.username}" />
      <property name="password" value="${jdbc.password}" />
      <property name="driverClassName"
         value="${jdbc.driverClassName}" />
      <property name="url" value="${jdbc.url}" />
   </bean>


   <!-- <bean id="offerDAO" class="kr.ac.hansung.csemall.OfferDAO"> -->
   <!-- <property name="dataSource" ref="dataSource"></property> -->
   <!-- offerDao라는 빈이생긴다. 생길때! 세터메소드가 불린다 그러면! OfferDAO클래스에 setDataSource메소드가!!!! 
      근데 그때 인자로써 dataSource의 빈이 들어간다는것! 그래서 jdbc템플릿 만들때 인자로 datasource가 들어간다는것! -->
   <!-- </bean> -->

   <context:annotation-config></context:annotation-config>
   <context:component-scan
      base-package="kr.ac.hansung.cse.dao"></context:component-scan>
   <!-- 저 패키지를 스캔해서 component가 있는 annotaion을 찾아서 빈으로 등록해주세요 같은 문장이다. -->



</beans>